# Task Plan: Bolo Code

## Goal
对齐 HC 日用：`bolo --resume` 无 id 项目列表已落地；下一刀斜杠 / 新会话。

## Next Step（默认）
**P0-b SL0–SL3**：斜杠总线 + `/help` `/compact` `/context` `/effort`…

## Current Phase
P0-a RS1–RS6 done — resume picker

## Priority snapshot
1. ~~P0-a resume 无 id 列表（HC）~~ ✅
2. P0-b slash 总线
3. P0-c BOLO 欢迎 TUI
4. P1 rules / cache / JSONL / creators
5. P2 subagent / MCP
6. P3 Electron

## Notes
- 执行勾选以 `docs/TODO.md` 为准
- 列表默认仅 `{cwd}/.bolo/sessions`